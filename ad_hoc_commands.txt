ansible-playbook update-by-group.yml --limit local:servers:kvm -C -v
ansible-playbook update-by-group.yml --limit kvm_test -C -v

# Upgrade all the CentOS yum servers.
ansible local:servers:kvm -m yum -a "name=* state=latest" -C -b

# Upgrade all the CentOS dnf servers.
ansible local:servers:kvm -m dnf -a "name=* state=latest" -C -b